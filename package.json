{
  "name": "reseller-order-tracking",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "firebase serve --port=5000",
    "login": "firebase login",
    "init": "firebase init",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "firebase": "^12.0.0"
  },
  "devDependencies": {
    "firebase-tools": "^14.11.2"
  }
}
